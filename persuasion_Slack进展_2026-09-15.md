# Tiger Game 项目进展（persuasion / CS298）

整理时间：2026-09-15  
来源：本地 Slack 工作区 `CS298` 的 `persuasion` 频道当前页面

> **范围更新：足球 / soccer 问题已标记为“忽略”。**  
> 该分支不再计入项目进度、任务计划或验收标准。当前唯一主要目标是完成 **Tiger Game 精确求解器**，并验证其背后的信念状态和分段线性 POMG 理论。

## 1. 主目标

完成一个可复现的 Tiger Game 精确求解器，用于：

- 计算有限期最优策略和价值函数。
- 展示价值函数的分段线性结构。
- 计算动作 Q 函数、交点和策略阈值。
- 验证贝叶斯信念更新与倾听次数之间的关系。
- 测试不同发射概率、奖励和期限下的收敛性。
- 为后续推广到更一般的 POMG 提供基础代码和实验框架。

## 2. 当前进展

### 已完成或已明确

- 项目方向已明确：机制设计、贝叶斯说服和部分可观测马尔可夫博弈。
- 核心理论已建立：
  - POMG 可转换为信念状态马尔可夫博弈。
  - 最优策略可以只依赖当前信念 `b_t`。
  - `Q(b, a)` 对信念是线性的。
  - `V(b) = max_a Q(b, a)` 是分段线性函数。
- Tiger Game 已作为统一基准案例。
- Tiger Game 原始论文和更易读的 POMDP 论文已在 Slack 中发布。
- 9 月 12 日的课堂笔记 `Persuasion_Notes_2.pdf` 已在 Slack 中发布。
- 已发现并更正一个关键参数：正确听音概率是 **85%**，不是会议口述的 75%。

### 尚未完成

- Slack 中还没有看到完整的 Tiger Game 求解器提交结果。
- 交点和斜率尚未按 85% 参数完整重新推导。
- 不同发射概率下的倾听次数和策略变化尚未形成系统实验。
- 期限增加时分段数量和非收敛现象尚未实现并复现。
- 从有限期 Tiger 特例到一般 POMG 求解框架的推广尚未开始。

## 3. Tiger Game 当前基线

### 博弈设定

- 状态：老虎在左或在右。
- 动作：打开左门、打开右门、倾听。
- 奖励：
  - 打开宝箱：`+10`。
  - 打开有老虎的门：`-100`。
  - 倾听：`-1`。
- 初始信念：`b = P(tiger on left) = 0.5`。

### 发射概率

- 老虎在左时，85% 听到“左边”，15% 听到“右边”。
- 老虎在右时，85% 听到“右边”，15% 听到“左边”。
- 9 月 4 日使用的 75% 已被明确更正，所有相关推导需要重做。

### 单期最后一步

- `Q(open left, b) = 10 - 110b`
- `Q(open right, b) = 110b - 100`
- `Q(listen, b) = -1`

交点为：

- `b = 0.1`
- `b = 0.9`

单期最优策略：

- `b < 0.1`：打开左门。
- `0.1 <= b <= 0.9`：继续倾听。
- `b > 0.9`：打开右门。

### 贝叶斯更新

在 85% 正确率下：

- 听到“左”：

  `b' = 0.85b / (0.85b + 0.15(1-b)) = 17b / (3 + 14b)`

- 听到“右”：

  `b' = 0.15b / (0.15b + 0.85(1-b)) = 3b / (17 - 14b)`

从 `b = 0.5` 开始：

- 一次听到“左”后：`b' = 0.85`。
- 再听到一次“左”后：`b' ≈ 0.9698`。

因此，在最后一步门槛仍为 `0.9` 的情况下，连续两次听到同一边即可越过开门阈值。

## 4. 下一步任务

### 第一优先级

- [ ] 阅读 Tiger Game 原始论文和 POMDP 参考论文。
- [ ] 按 85% 重新推导所有斜率、交点和倾听次数。
- [ ] 实现最小可运行的 Tiger Game 精确求解器。
- [ ] 输出每一期的策略、Q 函数、价值函数和分段数量。
- [ ] 用单元测试验证：
  - 最后一步阈值 `0.1` 和 `0.9`。
  - 贝叶斯更新公式。
  - 连续两次同向信号后的信念。
  - 有限期反向归纳结果。

### 第二优先级

- [ ] 测试发射概率 `0.85`、`0.70`、`0.65` 等参数。
- [ ] 解释不同参数下为什么需要不同次数的倾听。
- [ ] 增加期限并记录分段数量、运行时间和收敛情况。
- [ ] 复现“期限增加导致分段增长甚至不收敛”的现象。
- [ ] 形成可复现图表，展示最优策略和分段线性价值函数。

### 第三优先级

- [ ] 将 Tiger 特例抽象成可复用的 POMG 求解框架。
- [ ] 从两个状态扩展到三个状态，处理超平面和交点几何。
- [ ] 评估有限期与无限期求解的验收标准。

## 5. 开放问题

- 精确求解器最终只支持单玩家 POMDP，还是需要双人 POMG？
- 无限期情况下如何定义“精确解”与收敛标准？
- 动态部分的激励相容如何与 POMG 的策略遵守约束统一？
- 价值函数片段爆炸时，应给出精确解、近似解还是反例？
- 如何在代码中统一表示信念、发射矩阵、Q 函数和分段线性函数？

## 6. 已忽略分支

| 项目 | 状态 | 处理方式 |
|---|---|---|
| 足球 / soccer Markov Game 分析 | **忽略** | 不纳入当前任务、进度或验收 |
| 足球混合策略与 LP 退化案例 | **忽略** | 仅作为历史 Slack 资料保留 |
| 足球棋盘、球门宽度和移动失误实验 | **忽略** | 不再继续跟进 |
| Python 可复现脚本和 GitHub 仓库 | **忽略** | 不用于 Tiger Game 当前目标 |

说明：Slack 频道中最新出现了 Charith 的 35 页足球混合策略分析，但它属于已忽略分支。后续整理和汇报不再把该内容计入主要进度。

## 7. 本地文件

已统一放到 `C:\Users\24255\Desktop\298`：

| 文件 | 状态 | 用途 |
|---|---|---|
| `AAAI94-157_TigerGame.pdf` | 主目标 | Tiger Game 原始论文 |
| `aij98-pomdp.pdf` | 主目标 | POMDP 参考论文 |
| `Persuasion_Notes_2.pdf` | 主目标 | Tiger Game / persuasion 课堂笔记 |
| `CS298_Notes_0.pdf` | 参考 | 早期会议笔记 |
| `笔记 2026年9月4日 (2).pdf` | 参考 | 9 月 4 日手写笔记 |
| `positions.pdf` | 忽略 | 足球混合策略分析 |
| `Soccer_Notes.pdf` | 忽略 | 足球项目笔记 |

## 8. 状态结论

当前项目应以 Tiger Game 为唯一主线：

1. 先把参数更正为 85%。
2. 完成精确求解器。
3. 复现分段线性价值函数和策略阈值。
4. 再讨论向一般 POMG 推广。

足球 / soccer 分支已明确忽略，不进入后续计划。