# 项目进展整理（基于 2026-09-04 会议）

> 2026-09-15 范围更新：足球 / soccer 分支已标记为“忽略”，不作为当前目标；主要目标为 Tiger Game。Tiger 参数更正和最新进展见 `C:\Users\24255\Desktop\298\persuasion_Slack进展_2026-09-15.md`。本文保留为 9 月 4 日会议快照。

## 1. 一句话结论

项目目前处于**方案梳理与任务启动阶段**：整体研究路线已经从“机制设计”推进到“部分可观测马尔可夫博弈（POMG）与贝叶斯说服”，并明确了近期交付物是 **Tiger Game 精确求解器**。截至 9 月 4 日，工作区内尚无代码或论文草稿，当前可直接复用的材料主要是会议转录和手写笔记；9 月 15 日 Slack 中出现了足球混合策略的 GitHub/分析材料，但该分支已标记为忽略；当前主要目标仍是 Tiger Game 求解器。

## 2. 项目主线

### A. 机制设计与攻击可行性

- 常规问题：给定额外奖励设计，研究玩家会如何行动。
- 本项目反过来做：先给定希望玩家采取的目标行为，再设计奖励/支付，使其成为玩家的最优选择。
- 关注的是攻击方/信息设计方视角：不是最大化社会福利，而是研究如何改变玩家激励，使其执行指定动作。
- 玩家知道自己的类型 `theta`，机制设计者不知道类型，因此必须满足激励相容（IC）：
  - 玩家真实报告自己的类型时，收益不低于伪装成其他类型。
  - 安全型、风险型驾驶员或保险定价是典型例子。
- 主要数学条件：
  - 单调分配（monotonic allocation）。
  - 单交叉条件（single-crossing condition）。
  - IC、个体理性（IR）和一阶/二阶条件。
  - 连续类型分析用于理解结构；实际研究优先采用有限类型、有限分配的离散版本。
- 经典应用包括保险定价、垄断定价和价格歧视。
- 以前的工作假设分配 `Q` 已由目标策略 `pi_dagger` 指定，只设计支付 `P`。
- 曾考虑过同时设计 `Q` 与 `P`，但该项目一度搁置；目前会议主线仍以“目标行为已知，只设计激励/价格”为基准。
- 核心研究问题：**在什么条件下攻击可行？** 会议明确表示论文中超过一半篇幅可能围绕“何时可以攻击”。

### B. 部分可观测马尔可夫博弈与贝叶斯说服

- 在普通马尔可夫博弈的 `(S, A, R, P)` 上增加：
  - 观测集合 `O`。
  - 观测/发射矩阵 `E`，即给定状态时观察到某个信号的概率。
- 玩家不能直接观察真实状态，只能根据观测做决策。
- 贝叶斯说服的特点：设计者知道真实状态，并可以设计玩家看到的信号/观测，从而引导玩家采取目标动作。
- 在这个问题中，“设计观测”本质上就是设计发射矩阵 `E`。
- 股票好坏与“有前景/无前景”信号是标准贝叶斯说服例子。
- 部分可观测博弈的直接策略可能依赖完整历史，且一般需要无限记忆，无法直接写成有限状态算法。

#### 核心定理一：信念状态马尔可夫性

- 定义信念：

  `b_t(s) = P(S_t = s | 截至 t 的历史)`

- 最优策略可以只依赖当前信念 `b_t`，不必依赖完整历史。
- 因此，POMG 可以转换为“信念状态上的马尔可夫博弈”。
- 这是将不可直接求解的问题转换为标准动态规划问题的关键。

#### 核心定理二：价值函数的分段线性结构

- `Q(b, a)` 对信念是线性的。
- 价值函数是各动作 `Q` 的最大值：

  `V(b) = max_a Q(b, a)`

- 因此 `V` 是分段线性函数。
- 两个状态时，信念可压缩成一个标量 `b`，不同动作对应的直线交点就是策略阈值。
- 三个及以上状态时，直线变为高维超平面，需要处理向量、法向量、交点和几何求解。
- 精确求解思路：
  - 从最后一个时间步开始反向归纳。
  - 保留线性片段、斜率/法向量或交点。
  - 每次贝叶斯更新会生成新的信念点，并触发新的分段。
  - 理论上可精确求解，但片段数量可能随时间和状态指数增长。

#### Tiger Game 基准案例

- 两扇门，门后分别是老虎或宝箱。
- 动作：
  - 打开左门。
  - 打开右门。
  - 倾听一次，获取不完美信号。
- 奖励：
  - 打开宝箱：`+10`。
  - 打开有老虎的门：`-100`。
  - 倾听：`-1`。
- 发射矩阵（以 9 月 12 日 Slack 更正后的参数为准）：
  - 老虎在左时，85% 听到“左边”，15% 听到“右边”。
  - 老虎在右时同理。
  - 9 月 4 日会议口述的 75% 已被明确更正，不能再用于求解。
- 初始信念：左右各 50%。
- 单期最后一步的价值函数可直接算出：
  - `Q(open left, b) = 10 - 110b`
  - `Q(open right, b) = 110b - 100`
  - `Q(listen, b) = -1`
  - 其中 `b = P(tiger on left)`。
- 由此得到交点：
  - `b = 0.1`
  - `b = 0.9`
- 单期最优策略：
  - `b < 0.1`：开左门。
  - `0.1 <= b <= 0.9`：继续倾听。
  - `b > 0.9`：开右门。
- 贝叶斯更新（85% 正确率）：
  - 听到“左”后：`b' = 17b / (3 + 14b)`。
  - 听到“右”后：`b' = 3b / (17 - 14b)`。
  - 从 `b = 0.5` 出发，一次信号后得到 `0.85` 或 `0.15`。
  - 连续两次听到同一边，信念约为 `0.9698`，越过开门阈值 `0.9`。
- 特殊参数结果：
  - 会议随后将正确听音概率更正为 `0.85`；此前基于 `0.75` 的精确解和倾听次数必须重新核对。
  - 在新参数下，初始信念 `0.5` 连续两次听到同一方向会达到约 `0.9698`。
  - 会议还提到 `0.65` 的另一组特殊参数，但具体阈值和“听几次”需要以论文/代码核对。
- 一般参数下：
  - 不一定存在整洁的整数步阈值策略。
  - 增加时间步会增加线性片段数量。
  - Q iteration / value iteration 不保证收敛，数值稳定性也是主要难点。

## 3. 当前进度判断

| 方向 | 当前状态 | 证据/结论 |
|---|---|---|
| 总体研究问题 | 已定义 | 机制设计 + 贝叶斯说服 + 部分可观测动态博弈 |
| 机制设计基础 | 已讲解并建立 | IC、IR、单调分配、单交叉条件、离散类型 |
| POMG 理论框架 | 已搭建 | 信念状态马尔可夫性、分段线性价值函数 |
| Tiger Game 算例 | 已讲解理论解 | 已给出奖励、发射矩阵、Q 函数和阈值 |
| 精确求解器 | 尚未实现 | 工作区只有 PDF 笔记，没有代码仓库或实现文件 |
| 分组与负责人 | 尚未明确 | 会议提到一周后拆小组，但材料中没有名单 |
| 论文/代码资料 | 待发布 | 讲师承诺在群频道发布 Tiger 原论文和代码 |
| 风险分析 | 已识别 | 非收敛、片段爆炸、数值误差、动态均衡困难 |

## 4. 下一步任务

### 下周前

- [ ] 阅读群频道发布的 Tiger Game 原始论文和代码。
- [ ] 推导并记录 Tiger Game 的 Q 函数、交点和阈值。
- [ ] 写清为什么 `0.1` 和 `0.9` 是临界点。
- [ ] 推导贝叶斯更新公式，并解释更正后的 `0.85` 如何改变斜率。
- [ ] 实现一个最小可运行的 Tiger Game 求解器。
- [ ] 用不同发射概率测试：至少覆盖更正后的 `0.85`、`0.65` 和一般值（如 `0.70`）。
- [ ] 解释为什么不同参数下会出现不同的倾听次数。
- [ ] 测试时间步增加时片段数量如何变化，以及何时不收敛。
- [ ] 用公式和类结构组织代码，而不仅是一次性脚本。

### 第 3-4 周

- [ ] 将 Tiger 特例推广为可复用的精确 POMG 求解框架。
- [ ] 先完成两个状态的可视化和精确计算。
- [ ] 再评估三个状态的超平面、交点和数值几何问题。
- [ ] 记录收敛性、片段数量、运行时间等实验指标。
- [ ] 形成可复现的图表，展示最优策略和分段线性价值函数。

## 5. 关键开放问题

1. 当前主线是只设计支付 `P`，还是最终要同时设计分配 `Q` 和支付 `P`？
2. “可攻击”需要满足的最低条件是什么？单调分配是否必要，还是只是充分条件？
3. 机制设计中的 IC 如何与动态部分可观测博弈中的“策略遵守约束”统一？
4. 精确求解器最终要支持哪些模型：单玩家 POMDP、双人 POMG，还是更一般的动态机制？
5. 有限期求解与无限期求解应分别设定什么验收标准？
6. 当片段数量或策略不收敛时，论文采用精确结果、近似结果还是反例证明？
7. 各小组、软件负责人、论文负责人和下次汇报形式尚未在现有材料中明确。

## 6. 主要风险

- **理论风险**：一般 POMG 的最优策略可能需要无限记忆，不能简单用有限状态近似。
- **计算风险**：价值函数片段可能指数增长，导致时间与内存快速失控。
- **数值风险**：阈值、交点和贝叶斯更新容易产生误差，错误分段会污染后续所有迭代。
- **收敛风险**：Q iteration 和均衡求解都可能不收敛。
- **范围风险**：机制设计与 POMG 是两条较重的技术线，若不尽快拆分负责人，容易只停留在讲解层面。
- **资料风险**：论文、代码和分组信息尚未落到正式仓库或文档中。

## 7. 建议的近期执行方式

1. 把所有工作拆成两个明确小组：
   - 激励设计组：攻击可行性、IC/IR、单交叉与离散优化。
   - POMG 求解器组：信念更新、分段线性 DP、Tiger 基准和数值实验。
2. 先以 Tiger Game 作为统一验收标准，确保两组共享同一套符号和输出格式。
3. 建立最小代码仓库，目录至少包含 `models/`、`solvers/`、`experiments/`、`tests/`、`docs/`。
4. 每次会议只验收可运行结果或可推导公式，不再以“已听讲”作为进度。
5. 明确一个统一数据格式：状态、动作、转移、奖励、发射矩阵、折扣因子、时间跨度、目标策略。

## 8. 来源

- 用户提供的会议转录：`C:\Users\24255\.codex\attachments\950fc460-2839-4632-9eda-78646f17a52a\pasted-text.txt`
- 手写笔记：`C:\Users\24255\Desktop\298\笔记 2026年9月4日 (2).pdf`

说明：转录是自动语音识别结果，个别专有名词、数字和姓名可能有误；标为“待核对”的内容应以讲师之后发布的论文、代码和正式分组说明为准。